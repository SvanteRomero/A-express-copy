# Django Framework
django
django-extensions

# Django REST Framework for building APIs
djangorestframework

# NOTE: mysqlclient removed for Railway deployment (uses PostgreSQL)
# For local dev with MySQL, run: pip install mysqlclient

# Pillow for image processing
Pillow

# JSON Web Token Authentication for DRF
djangorestframework-simplejwt

# CORS Headers for allowing cross-origin requests
django-cors-headers

# WSGI HTTP Server for production deployment
gunicorn

# Django Filter for powerful filtering against querysets
django-filter

# Manages environment variables and .env files
python-decouple
python-dotenv

# Django Channels for WebSocket support
channels
channels-redis

pytz

# Production deployment
dj-database-url
psycopg2-binary
whitenoise

# Security - Rate limiting and brute-force protection
django-axes
django-ratelimit

# S3 Storage for production media files
django-storages[s3]
boto3
